<html lang="en">
  {{> "head/head"}}
  <body>
      <div class="modal-overlay">{{> "icons/close-btn"}}</div>
      {{> "header/header"}}
      <main>
        {{{body}}}
      </main>
      {{> "footer/footer"}}
  </body>
</html>
